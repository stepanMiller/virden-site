# Font assets

No commercial font files are included at stage 1.

If VIRDEN has licensed corporate fonts, provide webfont files (`woff2`) and usage rights. They can replace the temporary serif/sans stacks through `next/font/local` without changing the page architecture.

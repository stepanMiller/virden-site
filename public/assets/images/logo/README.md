# VIRDEN logo assets

Place the approved original logo here. Preferred handoff:

- `virden-logo.svg` — primary version;
- `virden-logo-light.svg` — only if an approved light version exists;
- PNG fallbacks at 2× size if required.

The current UI renders an explicit asset slot. It does not redraw, approximate or generate the VIRDEN mark.

# Homepage hero video assets

Expected approved files:

- `virden-velvet-desktop.webm`
- `virden-velvet-mobile.webm`
- optional MP4 fallbacks if the delivery matrix requires them
- a separate approved poster image

After delivery, add the paths to `content/home.ts`. Keep all typography, logo, navigation and CTAs outside the video file.

Recommended delivery targets: muted loop, no audio track, short duration, optimized bitrate, dimensions matched to the intended crop, and a visually close first frame/poster to prevent a flash during loading.
